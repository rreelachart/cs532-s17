This directory holds the collection of tweets from various sources.

They were gathered by running: 
"python getTweets.py <starting tweet ID><number of tweets per call><number of api calls><screenname to find>"
Then redirecting the output to a text file with the ">" character.

All tweet collections were run with the same number of tweets per call <200) and number of api calls (3).

The following sources were used to collect tweets:
Guillermo del Toro
James Gunn
Kelly Turnbull
Seanbaby
John Carmack
Red Letter Media
Film Crit Hulk
